openpty:
	Nobody knows how much space should be reserved for name.  So, calling openpty() or forkpty() with non-NULL name may not be secure.

start_color:
	COLOR_YELLOW is actually brown.  To get yellow, use COLOR_YELLOW combined with the A_BOLD attribute.

SDL_SetColorKey:
	-ne fonctionne qu'avec des image 24bits, mais cela n'est mentionne nul part.

gettimeofday:
	If tzp is not a null pointer, the behavior is unspecified.
